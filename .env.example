# Copia a un solo archivo: .env
# cp .env.example .env
#
# Error 28P01 / "password authentication failed"
# → Usa la contraseña real de PostgreSQL (no dejes TU_PASSWORD).

DATABASE_URL=postgresql://postgres:TU_PASSWORD@localhost:5432/chuichun_db
